# Offline Docs

Codelex intentionally avoids full documentation mirrors.

The built-in offline bundle is the curated snippet database:

- syntax forms
- common aliases
- related concept links
- session-local usage history

All lookup, compare, session, and TUI workflows run without network access after dependencies are built.
